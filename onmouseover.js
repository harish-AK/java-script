//when the cursor points the image it will pop up when cursor move aways its get back to orginal size
function changesize(){
     document.getElementById("img").style.width="500px"; //.src="path"
}

function changesize2(){
    document.getElementById("img").style.width="250px";  //.src="path" //250 is the original size 2nd function used to get back the image in previous size
}

//we can use the source  instead of style to change the image to another image where cursor points the image 